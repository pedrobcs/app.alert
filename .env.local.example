# SafeAlert Configuration
#
# SECURITY NOTICE:
# ================
# Variables prefixed with NEXT_PUBLIC_ are exposed to the browser (client-side).
# Variables without this prefix are ONLY available on the server-side.
# NEVER prefix sensitive credentials (API keys, secrets, tokens) with NEXT_PUBLIC_
#
# To use these variables:
# 1. Copy this file to .env.local
# 2. Fill in your actual values
# 3. .env.local is gitignored and will never be committed

# ============================================================================
# CLIENT-SIDE VARIABLES (NEXT_PUBLIC_*)
# These are safe to expose in the browser
# ============================================================================

# API Base URL (required)
# Example: https://your-ngrok-url.ngrok.io
NEXT_PUBLIC_API_BASE_URL=

# Emergency Contact Numbers (optional - defaults to +15085140864)
# Format: E.164 international format (e.g., +15551234567)
NEXT_PUBLIC_CONTACT_1=+15085140864

# You can add more contacts if needed:
# NEXT_PUBLIC_CONTACT_2=+15551234568
# NEXT_PUBLIC_CONTACT_3=+15551234569

# ============================================================================
# SERVER-SIDE VARIABLES (SENSITIVE - NO NEXT_PUBLIC_ PREFIX)
# These are NEVER exposed to the browser
# ============================================================================

# Twilio Configuration (for WhatsApp/SMS notifications)
# Get these from: https://console.twilio.com/
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# Additional server-side configuration
# NODE_ENV=production
